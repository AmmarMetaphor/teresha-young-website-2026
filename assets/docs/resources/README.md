# Resources PDFs (expected, not yet supplied)

This directory is a scaffold for the two approved resource guides referenced
on `resources.html`. The actual PDF files do not exist yet and must not be
fabricated — see `docs/open-decisions.md`.

Expected filenames, once real files are supplied:

- `igniting-intimacy-12-powerful-questions.pdf`
- `7-day-me-time-plan.pdf`

When a real file is added here, update the matching card in `resources.html`:
replace the disabled `<span class="resource-card__action" aria-disabled="true">`
with a real `<a href="assets/docs/resources/<filename>" target="_blank"
rel="noopener" class="resource-card__action">`.
